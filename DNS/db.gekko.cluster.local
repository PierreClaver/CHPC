$ORIGIN .
$TTL 86400	; 1 day
gekko.cluster.local	IN	SOA	headnode.gekko.cluster.local	root.localhost. (
					0	; serial
					604800	; refresh (1 week)
					86400	; retry (1 day)
					2419200	; expire (4 weeks)
					86400	; minimum (1 day)
					)
			NS		headnode.gekko.cluster.local.
$ORIGIN gekko.cluster.local.
headnode		IN	A	10.0.0.1
node1			IN	A	10.0.0.2
node2			IN	A	10.0.0.3
